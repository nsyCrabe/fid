const CARDS_KEY='loyalty_cards_v1',THEME_KEY='loyalty_theme_v2';
const COLORS=['#2264d1','#8b5cf6','#e05252','#e88b27','#168c65','#008da8','#d04d91','#475569'];
const THEMES=[{id:'light',name:'Clair',category:'light',description:'Propre et lumineux',swatch:'linear-gradient(135deg,#f4f7fb,#3268e8)'},{id:'sunset',name:'Coucher de soleil',category:'light',description:'Chaleureux et doux',swatch:'linear-gradient(135deg,#fff5f1,#ed6d5b)'},{id:'forest',name:'Forêt',category:'light',description:'Nature et calme',swatch:'linear-gradient(135deg,#eef8f2,#27975d)'},{id:'ocean',name:'Océan',category:'light',description:'Frais et moderne',swatch:'linear-gradient(135deg,#edf9fc,#008fa8)'},{id:'candy',name:'Bonbon',category:'light',description:'Rose et léger',swatch:'linear-gradient(135deg,#fff3f8,#ed5594)'},{id:'midnight',name:'Minuit',category:'dark',description:'Confortable le soir',swatch:'linear-gradient(135deg,#0e1525,#5c8df6)'},{id:'graphite',name:'Graphite',category:'dark',description:'Gris profond',swatch:'linear-gradient(135deg,#111318,#aeb8c7)'},{id:'purple-night',name:'Nuit violette',category:'dark',description:'Sombre et élégant',swatch:'linear-gradient(135deg,#151022,#b889ff)'},{id:'nord',name:'Nord',category:'dark',description:'Bleu ardoise',swatch:'linear-gradient(135deg,#202733,#88c0d0)'},{id:'ember',name:'Braise',category:'dark',description:'Sombre et chaleureux',swatch:'linear-gradient(135deg,#211414,#ff9b68)'},{id:'deep-ocean',name:'Océan profond',category:'dark',description:'Sombre et cyan',swatch:'linear-gradient(135deg,#071b27,#5ed6e6)'},{id:'mono',name:'Noir et blanc',category:'dark',description:'Minimaliste',swatch:'linear-gradient(135deg,#101010,#fff)'}];
let cards=[],editingId=null,openedId=null,selectedColor=COLORS[0],selectedLogo=null,stream=null,detectorInterval=null;const $=id=>document.getElementById(id);
function load(){try{cards=JSON.parse(localStorage.getItem(CARDS_KEY)||'[]')}catch{cards=[]}setTheme(localStorage.getItem(THEME_KEY)||'light',false);$('quickGuide').hidden=localStorage.getItem('loyalty_hide_guide')==='1'}function save(){localStorage.setItem(CARDS_KEY,JSON.stringify(cards))}function setTheme(theme,persist=true){document.body.dataset.theme=theme;if(persist)localStorage.setItem(THEME_KEY,theme);const colors={light:'#f4f7fb',midnight:'#0e1525',graphite:'#111318','purple-night':'#151022',nord:'#202733',ember:'#211414','deep-ocean':'#071b27',sunset:'#fff5f1',forest:'#eef8f2',ocean:'#edf9fc',candy:'#fff3f8',mono:'#101010'};$('themeColorMeta').content=colors[theme]||colors.light;renderThemes()}
function uid(){return`card_${Date.now()}_${Math.random().toString(36).slice(2,8)}`}function initials(n){return n.trim().slice(0,2).toUpperCase()}function esc(v){const e=document.createElement('span');e.textContent=v;return e.innerHTML}function typeLabel(t){return t==='barcode'?'Code-barres':t==='qrcode'?'QR code':'Sans code'}function darken(h,a=-30){const n=parseInt(h.replace('#',''),16),c=v=>Math.max(0,Math.min(255,v+a));return`#${((1<<24)+(c(n>>16)<<16)+(c(n>>8&255)<<8)+c(n&255)).toString(16).slice(1)}`}
function render(){const g=$('cardsGrid'),q=$('searchInput').value.trim().toLowerCase(),f=cards.filter(c=>c.name.toLowerCase().includes(q));g.innerHTML='';$('cardCount').textContent=`${cards.length} carte${cards.length===1?'':'s'}`;$('emptyState').hidden=cards.length!==0||q.length===0;if(cards.length===0)$('emptyState').hidden=false;if(!f.length&&cards.length){g.innerHTML='<p class="no-result">Aucune carte trouvée.</p>';return}f.forEach((c,i)=>{const b=document.createElement('button');b.className='loyalty-card';b.type='button';b.style.setProperty('--card-color',c.color||COLORS[0]);b.style.setProperty('--card-color-dark',darken(c.color||COLORS[0]));b.style.animationDelay=`${i*45}ms`;b.innerHTML=`<div class="card-logo">${c.logo?`<img src="${c.logo}" alt="Logo ${esc(c.name)}">`:esc(initials(c.name))}</div><div><p class="card-name">${esc(c.name)}</p><p class="card-type">${typeLabel(c.codeType)}</p></div><span class="card-arrow">›</span>`;b.addEventListener('click',()=>openDetail(c.id));g.appendChild(b)})}
function showToast(m){const old=$('toast');if(old)old.remove();const t=document.createElement('div');t.id='toast';t.className='toast';t.textContent=m;document.body.appendChild(t);setTimeout(()=>t.remove(),2200)}function showForm(id=null){editingId=id;const c=id?cards.find(x=>x.id===id):null;$('formTitle').textContent=c?'Modifier la carte':'Nouvelle carte';$('nameInput').value=c?.name||'';$('codeInput').value=c?.code||'';$('codeTypeInput').value=c?.codeType||'barcode';selectedColor=c?.color||COLORS[0];selectedLogo=c?.logo||null;updateLogoPreview();renderColors();openLayer('formSheet','formBackdrop')}
function closeForm(){closeLayer('formSheet','formBackdrop');editingId=null}function openLayer(p,b){$(p).hidden=false;$(b).hidden=false;document.body.classList.add('no-scroll')}function closeLayer(p,b){$(p).hidden=true;$(b).hidden=true;document.body.classList.remove('no-scroll')}
function renderColors(){$('colorRow').innerHTML=COLORS.map(c=>`<button type="button" class="color-dot ${c===selectedColor?'selected':''}" style="background:${c}" data-color="${c}" aria-label="Choisir la couleur"></button>`).join('');$('colorRow').querySelectorAll('.color-dot').forEach(d=>d.addEventListener('click',()=>{selectedColor=d.dataset.color;renderColors()}))}
function updateLogoPreview(){const p=$('logoPreview');if(selectedLogo){p.innerHTML=`<img src="${selectedLogo}" alt="Logo sélectionné">`;$('removeLogo').hidden=false}else{p.innerHTML='<span>🏪</span>';$('removeLogo').hidden=true}}
function resizeLogo(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=reject;reader.onload=e=>{const image=new Image();image.onload=()=>{const s=320,c=document.createElement('canvas');c.width=s;c.height=s;const ctx=c.getContext('2d');ctx.fillStyle='#fff';ctx.fillRect(0,0,s,s);const k=Math.min(s/image.width,s/image.height),w=image.width*k,h=image.height*k;ctx.drawImage(image,(s-w)/2,(s-h)/2,w,h);resolve(c.toDataURL('image/jpeg',.84))};image.onerror=reject;image.src=e.target.result};reader.readAsDataURL(file)})}
function saveCard(e){e.preventDefault();const name=$('nameInput').value.trim();if(!name)return showToast('Ajoute le nom du magasin.');const d={name,code:$('codeInput').value.trim(),codeType:$('codeTypeInput').value,color:selectedColor,logo:selectedLogo};if(editingId)Object.assign(cards.find(c=>c.id===editingId),d);else cards.push({id:uid(),...d,createdAt:Date.now()});try{save()}catch{return showToast('Stockage plein : choisis un logo plus léger.')}render();closeForm();showToast('Carte enregistrée ✓')}
function openDetail(id){openedId=id;const c=cards.find(x=>x.id===id);if(!c)return;$('detailName').textContent=c.name;$('detailType').textContent=typeLabel(c.codeType);$('detailCode').textContent=c.code||'';$('detailLogo').innerHTML=c.logo?`<img src="${c.logo}" alt="Logo ${esc(c.name)}">`:esc(initials(c.name));$('barcodeCanvas').style.display='none';$('qrHolder').innerHTML='';if(c.code&&c.codeType==='barcode'&&window.JsBarcode){try{$('barcodeCanvas').style.display='block';JsBarcode('#barcodeCanvas',c.code,{format:'CODE128',displayValue:false,height:82,width:2,margin:5})}catch{}}if(c.code&&c.codeType==='qrcode'&&window.QRCode)new QRCode($('qrHolder'),{text:c.code,width:190,height:190});$('detailPanel').hidden=false;$('detailBackdrop').hidden=false;document.body.classList.add('no-scroll')}
function closeDetail(){ $('detailPanel').hidden=true; $('detailBackdrop').hidden=true; document.body.classList.remove('no-scroll'); openedId=null; }
function renderThemes(){const current=document.body.dataset.theme;const section=(cat,title,description)=>{const items=THEMES.filter(t=>t.category===cat).map(t=>`<button type="button" class="theme-option ${t.id===current?'active':''}" data-theme="${t.id}"><span class="theme-swatch" style="background:${t.swatch}"></span><span class="theme-info"><strong>${t.name}</strong><small>${t.description}</small></span><span class="theme-check">${t.id===current?'✓':''}</span></button>`).join('');return`<div class="theme-category"><div class="theme-category-heading"><h3>${title}</h3><p>${description}</p></div><div class="theme-category-list">${items}</div></div>`};$('themeList').innerHTML=section('light','☀️ Clair','Des couleurs lumineuses')+section('dark','🌙 Sombre','Confortable dans le noir');$('themeList').querySelectorAll('.theme-option').forEach(i=>i.addEventListener('click',()=>setTheme(i.dataset.theme)))}
async function openScanner(){if(!('BarcodeDetector'in window)||!navigator.mediaDevices?.getUserMedia)return showToast('Scanner indisponible : saisis le code manuellement.');$('scanner').hidden=false;try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});$('scanVideo').srcObject=stream;const detector=new BarcodeDetector({formats:['qr_code','code_128','ean_13','ean_8','upc_a','upc_e','code_39']});detectorInterval=setInterval(async()=>{try{const result=await detector.detect($('scanVideo'));if(!result.length)return;$('codeInput').value=result[0].rawValue;$('codeTypeInput').value=result[0].format==='qr_code'?'qrcode':'barcode';closeScanner();showToast('Code trouvé ✓')}catch{}},350)}catch{closeScanner();showToast('Autorise la caméra pour scanner.')}}function closeScanner(){$('scanner').hidden=true;if(detectorInterval)clearInterval(detectorInterval);detectorInterval=null;if(stream)stream.getTracks().forEach(track=>track.stop());stream=null}
$('addButton').addEventListener('click',()=>showForm());$('emptyAddButton').addEventListener('click',()=>showForm());$('closeForm').addEventListener('click',closeForm);$('cancelForm').addEventListener('click',closeForm);$('formBackdrop').addEventListener('click',closeForm);$('cardForm').addEventListener('submit',saveCard);$('searchInput').addEventListener('input',render);$('hideGuide').addEventListener('click',()=>{$('quickGuide').hidden=true;localStorage.setItem('loyalty_hide_guide','1')});$('chooseLogo').addEventListener('click',()=>$('logoInput').click());$('logoPreview').addEventListener('click',()=>$('logoInput').click());$('logoInput').addEventListener('change',async event=>{const file=event.target.files[0];if(!file)return;try{selectedLogo=await resizeLogo(file);updateLogoPreview();showToast('Logo ajouté ✓')}catch{showToast('Image impossible à utiliser.')}event.target.value=''});$('removeLogo').addEventListener('click',()=>{selectedLogo=null;updateLogoPreview()});$('themeButton').addEventListener('click',()=>{renderThemes();openLayer('themeSheet','themeBackdrop')});$('closeTheme').addEventListener('click',()=>closeLayer('themeSheet','themeBackdrop'));$('themeBackdrop').addEventListener('click',()=>closeLayer('themeSheet','themeBackdrop'));$('closeDetail').addEventListener('click',closeDetail);$('closeDetailButton').addEventListener('click',closeDetail);$('detailBackdrop').addEventListener('click',closeDetail);$('editCard').addEventListener('click',()=>{const id=openedId;closeDetail();showForm(id)});$('deleteCard').addEventListener('click',()=>{if(!openedId||!confirm('Supprimer cette carte ?'))return;cards=cards.filter(card=>card.id!==openedId);save();render();closeDetail();showToast('Carte supprimée.')});$('scanButton').addEventListener('click',openScanner);$('closeScanner').addEventListener('click',closeScanner);
load();render();
